package heap

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestHeap(t *testing.T) {
	// foo := NewList[int]()
	// assert.Equal(t, foo.ToString(), "[ ]", "construct an empty list")
	assert.Equal(t, "foo", "foo", "placeholder")
}
